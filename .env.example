# Sanity project — public, safe to embed in the client bundle.
# Copy this file to `.env` for local development.
# In CI, these are set as GitHub Actions repository variables on the workflow.
PUBLIC_SANITY_PROJECT_ID=5b75k1rw
PUBLIC_SANITY_DATASET=production
